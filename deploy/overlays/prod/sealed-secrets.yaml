# Placeholder — generate with `kubeseal` against ../../base/secrets.example.yaml
# and commit the resulting SealedSecret objects here. Do NOT commit plaintext Secrets.
apiVersion: v1
kind: List
items: []
